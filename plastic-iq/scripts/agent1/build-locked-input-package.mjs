/**
 * Phase 5 — CLI shim for locked input package builder.
 */
export {
  buildLockedInputPackage,
  checkLockEligibility,
  LockEligibilityError,
  METHODOLOGY_VERSION,
  MATERIAL_LOOKUP_VERSION,
} from '../../src/lib/lockedInput/buildLockedInputPackage.ts'
